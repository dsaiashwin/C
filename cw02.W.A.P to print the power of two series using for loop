#include <stdio.h>
int main()
{
    int u,n,res=1;
    scanf("%d",&u);
    
    if (u >0 )
    {printf("%d ",res);
    for (n=2;n<=u;n++)
        {
            res=res*2;
            printf("%d ",res);
        }
    }
    else 
    {
        printf("Error : Number should be an positive number.");
    }
    return 0;
}
