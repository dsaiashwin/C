#include <stdio.h>
int main()
{ int n,i,c,j;
    scanf("%d",&n);
    if (n > 0)
    {
        for (i = 2 ; i <= n ; i++)
        {   c=0;
            for (j=1; j <= ((i/2)+1) ; j++ )
            { 
                if (i%j == 0)
                {
                    c+=1;
                }
            }   if (i > 2)
                {
                    if (c < 2 )
                    printf("%d,",i);
                }
                else if (i <= 2)
                {
                    printf("%d,",i);
                }
        }
    }   
}
