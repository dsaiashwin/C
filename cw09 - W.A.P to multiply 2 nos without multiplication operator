#include <stdio.h>
int main()
{
    int n1,n2,res=0;
    //printf("Enter the two numbers : "");
    scanf("%d %d",&n1,&n2);
    
    for (int i=0; i < n2 ; i++)
    {
        res=res+n1;
    }
    printf("The result is %d",res);
}
