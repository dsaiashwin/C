#include <stdio.h>
int main()
{
    int n,n1,n2,t;
    
    scanf("%d",&n);
    n1=0;                       
    n2=1;                       
    if (n < 0)
    printf("Invalid input");
    
    else if (n==0)
    {
        printf("0");
    }
    else if (n > 0)
    {printf("%d %d ",n1,n2);
        t=0;
        while (n >= t)
        {
        
        t=n1+n2;
        n1=n2;
        n2=t;
        if(n >= t)
        {printf("%d ",n2);}
        }
    }
}
