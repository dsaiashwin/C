#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    if (num > 50 && num < 101)
    {
        printf("%d is in range",num);
    }
    else
    {
        printf("%d is not in range",num);
    }
}
